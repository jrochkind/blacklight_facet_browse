<%= form_tag({}, :method => :get) do %>
  <%= text_field_tag @browse_config.query_param_name, params[@browse_config.query_param_name], :class => "q", :id => "q", :placeholder => "starting with…", :autocomplete => "off" %>
  <%= submit_tag t('blacklight.search.form.submit'), :class=>'submit' %>

  <%= search_as_hidden_fields(:omit_keys => [@browse_config.query_param_name, :page, :id, :'catalog_facet.offset', :commit]).html_safe %>        

<% end %>
  